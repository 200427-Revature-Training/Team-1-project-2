server:
  port: 4444
spring:
  application:
    name: concert-events   
  datasource:
    url: jdbc:postgresql://${YUKAJUCO_URL}/yukajuco
    username: ${YUKAJUCO_U}
    password: ${YUKAJUCO_P}
    driver-class-name: org.postgresql.Driver  
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQL95Dialect
    show-sql: true
    hibernate:
      ddl-auto: update